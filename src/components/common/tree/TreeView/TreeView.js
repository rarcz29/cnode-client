import { TreeView as MaterialTreeView } from '@material-ui/lab';
import React from 'react';

const TreeView = (props) => {
    return <MaterialTreeView {...props}></MaterialTreeView>;
};

export default TreeView;
