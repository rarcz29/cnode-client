import React from 'react';
import styled from 'styled-components';

const StyledLogo = styled.h1`
    font-size: 24px;
`;

const Logo = () => {
    return <StyledLogo>.gitnode</StyledLogo>;
};

export default Logo;
