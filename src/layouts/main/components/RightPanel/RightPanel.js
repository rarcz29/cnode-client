import React from 'react';
import { Container } from './style';

const RightPanel = ({ className }) => {
    return <Container className={className}>asdfasdf</Container>;
};

export default RightPanel;
