const USER_DATA_NAME = 'user';
const PLATFORM_NAME = 'platform';

const GITHUB_VALUE = 'github';
const BITBUCKET_VALUE = 'bitbucket';
const GITLAB_VALUE = 'gitlab';

const LOCAL_STORAGE = {
    USER_DATA_NAME,
    PLATFORM_NAME,
    GITHUB_VALUE,
    BITBUCKET_VALUE,
    GITLAB_VALUE,
};

export default LOCAL_STORAGE;
